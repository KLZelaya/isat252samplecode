/**
 * a function to add two numbers
 * @param {Number} a The first number to be added
 * @param {Number} b The second number to be added
 * @param {Number} c The sum of a and b
 */
let sum = (a,b) => {
	return a + b;
};

module.exports = sum; //thing on the right hand side should be the thing above it

//-----------------alternatives----------------------------
//
// function sum(a, b) {
// 	return a + b;
// }

// let sum = function(a,b) {
// 	return a + b;
// };

// let sum = (a,b) => a + b;

// module.exports = (a,b) => a + b;
//all of these things are the same things ^^^^

